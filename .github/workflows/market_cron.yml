name: Market Scanner & AI Signal Screener Daily

on:
  schedule:
    # Ranní sken v 7:50 před otevřením trhů (vyhýbá se frontám v celou hodinu :00 na GitHubu)
    # 05:50 UTC v letním čase (SELČ = 07:50) a 06:50 UTC v zimním čase (SEČ = 07:50)
    - cron: '50 5 * * *'
    - cron: '50 6 * * *'
  workflow_dispatch: # Umožňuje okamžitý manuální refresh kdykoliv přímo z GitHubu

  push:
    branches:
      - main
      - master

# Nastavení oprávnění pro GitHub Pages a uložení aktualizovaného index.html do repozitáře
permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: market-scanner-cron
  cancel-in-progress: false

jobs:
  scan-and-publish:
    name: Market Scan & Publish to GitHub Pages + Forpsi
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: 📦 Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 🤖 Run Market Scanner & AI Analysis
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_MODEL: gemini-3.5-flash-lite
        run: |
          python main.py

      - name: 💾 Commit & Push updated index.html & history snapshots to Repository
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add index.html data/history/
          git diff --staged --quiet || (git commit -m "Auto-update market screener report & history snapshot [skip ci]" && git push)

      - name: 🌐 Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: 📤 Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - name: 🚀 Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: 🌐 Optional Deploy to Forpsi Hosting via FTP
        if: env.FTP_SERVER != ''
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./
          server-dir: /www/

